[package]
name = "crash-round"
version = "0.1.0"
edition = "2021"

[dependencies]
ckb-std = "1.1"
sha2 = { version = "0.10", default-features = false }

[features]
library = []
native-simulator = ["library", "ckb-std/native-simulator"]
