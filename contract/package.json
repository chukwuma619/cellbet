{
  "name": "@cellbet/contract",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "build": "cargo build --release",
    "test": "cargo test",
    "check": "cargo check",
    "lint": "cargo clippy -- -D warnings",
    "fmt": "cargo fmt --check"
  }
}
